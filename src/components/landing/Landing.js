import React from 'react';
import Rocket from '../images/rocket2.gif';

const Landing = () => {
    return (
        <div>
            <img src={Rocket} className="App-logo" alt="logo" />
        </div>
    ); 
}

export default Landing;